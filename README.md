```bash
# This works - use python
python -m venv venv

# Activate venv (Windows CMD/PowerShell)
venv\Scripts\activate

# Or if you're using Git Bash or Cmder (which you are)
source venv/Scripts/activate
```
